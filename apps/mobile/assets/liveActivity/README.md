# Live Activity assets

Place PNGs here for iOS Lock Screen / Dynamic Island (optional).

Each file must be **under 4 KB** (Apple limit). If you add `imageName` in code, use filenames without extension in state (e.g. `live_activity` → `live_activity.png`).

After changing assets, run `npx expo prebuild --clean` and rebuild the dev client.
